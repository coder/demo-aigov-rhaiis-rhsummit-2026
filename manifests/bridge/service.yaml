---
apiVersion: v1
kind: Service
metadata:
  name: bridge
  namespace: coder
  labels:
    app.kubernetes.io/name: bridge
    app.kubernetes.io/part-of: coder-aigov-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bridge
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
